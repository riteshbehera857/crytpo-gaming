import attachCurrentUser from './attachCurrentUser';
import isAuth from './isAuth';
import googleVerifyToken from './googleVerifyToken';

export default {
  attachCurrentUser,
  isAuth,
  googleVerifyToken,
};
